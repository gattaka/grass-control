#JSGF V1.0;

grammar all;
public <intro> = grass control;
public <commands> = <intro> (open <targets> | <vlc-commands> | volume (up | down));
<targets> = grass | nexus | hardware | articles | system monitor | speech history;
<vlc-commands> = player (next | previous | stop | play | (start | stop) shuffle | status);

/*
public <misc-player> = payer | prayer;

public <misc-next> = sex | flex | text | test | eggs;
public <misc-previous> = hideous;

public <misc-play> = stay | way | may | day | clay;
public <misc-stop> = top | flop | crop; 
public <misc-volume> = kingdom | demon;
public <misc-shuffle> = ruffle;
public <misc-status> = wait us | paint us;

public <misc-grass> = gross | floss | pass | mass;
public <misc-control> = fall | stall | call;
public <misc-open> = oven;
public <misc-nexus> = us;
public <misc-hardware> = spare;
public <misc-articles> = miss;
*/