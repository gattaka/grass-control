#JSGF V1.0;

grammar all;
public <commands> = grass control (open <targets>);
<targets> = (grass | nexus | hardware | articles | calculator | you tube);

public <vlc> = v l c player <vlc-commands>;
<vlc-commands> = (next | previous | stop | play);

<misc> = gross | pay | payer;