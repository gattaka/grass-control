#JSGF V1.0;

grammar all;
public <commands> = grass control (open <targets> | <vlc-commands> | volume (up | down));
<targets> = (grass | nexus | hardware | articles | system monitor | speech history);
<vlc-commands> = player (next | previous | stop | play | (start | stop) shuffle | status);