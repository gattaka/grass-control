#JSGF V1.0;

grammar all;
public <commands> = grass control (open <targets>);
<targets> = (hardware | articles | calculator | you tube);

public <vlc> = v l c <vlc-commands>;
<vlc-commands> = (next | previous | stop | play);